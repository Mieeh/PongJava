#version 330 core

layout(location = 0) in vec3 in_position;
layout(location = 1) in vec3 in_color;

uniform mat4 projection_matrix;

out vec3 color;

void main()
{

    gl_Position = projection_matrix * vec4(in_position.x, in_position.y, in_position.z, 1.0);

    color = vec3(1, 0, 0);

}